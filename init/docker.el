;;--------------------------------;
;; Docker
;;--------------------------------;

(use-package docker
  ;; :ensure t
  )
(use-package docker-compose
  ;;:ensure t
  )
(use-package docker-tramp
  ;; :ensure t
  )
(use-package dockerfile-mode
  ;; :ensure t
  )
